<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme" />


    <style name="Theme.Application" parent="Theme.AppCompat">

        <!-- Main theme colors -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/white</item>
        <item name="colorButtonNormal">@color/primary</item>

    </style>


    <style name="Theme.Application.NoActionBar" parent="Theme.Application">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds" tools:targetApi="lollipop">true</item>
        <item name="android:statusBarColor" tools:targetApi="lollipop">@android:color/transparent
        </item>
    </style>

    <style name="Widget.Application.ActionBar" parent="Widget.AppCompat.ActionBar.Solid">
        <item name="displayOptions">showTitle</item>
        <item name="android:windowActionModeOverlay">true</item>
        <item name="actionModeBackground">@color/accent</item>
        <!-- <item name="elevation">0dp</item>-->
    </style>
</resources>
